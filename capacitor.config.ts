import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'gocircular-desktopfrontend',
  webDir: 'dist'
};

export default config;
